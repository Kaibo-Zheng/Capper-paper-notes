# 做AI的科研，复现论文的第一步一定是要拿到数据集，拿不到数据集没有任何复现的必要

拿到数据是复现论文的第一步，确保这些数据是完全可以拿到的，以及是准确无误的。然后复现一篇论文的时间不要超过1周，超过一周说明文章的复现有困难，在确定自身的科研水平的情况下，只能说明这篇论文设计之初就没让你复现，可能出于各种原因。

# 拿到数据之后一定要了解数据，了解数据的构成，数据量，数据里面都有什么内容，是干什么的

作为数据科学家，一定要了解数据本身，这一个最基本的数据单元都包含什么信息，AI的本质就是对数据进行挖掘。所以数据挖掘是十分重要的。数据里面存储什么信息，乃至于数据量有多大，每一个信息都不可以忽视，数据本身的每个元素的重要性有大有小，但是基本不会存在一点意义没有的数据。
一个好的AI科学家，它的数据归档整理和分析一定是十分到位的。
什么算你对这个项目的数据了解清晰了，就是说这个项目的数据目录里面每个文件是干嘛的你都要清楚。

# 模型架构的设计，模型架构的设计一定是要符合数据结构本身特点，这也是从数据的分析角度去出发相较更为合理的地方。

# 代码的结构

如果你不是土豪，建议还是先了解一下你的机器的性能的极限有多少尽可能发挥你机器的极限，尽量不要出现什么显卡等CPU的问题，时间就是金钱，效率就是生命，小平同志说的好，我们节省下来的训练时间，可以用到我们远远不断地新的训练中去。项目可读性要好。每个模块里面也写一个readme，就是介绍一下这个里面的结构。还是保证你对自己的项目有一个清晰的认知和理解。

GPU 型号、数量、显存、CUDA/Driver、GPU 拓扑
CPU 型号、核心数、线程数
内存容量、swap 情况
磁盘容量、类型、IO 速度
网络带宽，尤其是多机训练时
系统版本、Python/PyTorch/CUDA 版本

GPU 利用率
显存占用
功耗和温度
CPU 使用率
内存和 swap
磁盘 IO
samples/sec 或 steps/sec
多卡之间的利用率是否均衡

最大 batch size 能到多少？
GPU 能不能长期 80% 以上利用率？
显存是否成为瓶颈？
DataLoader 是否拖慢训练？
磁盘 IO 是否拖慢训练？
多卡加速比是否接近理想？
训练速度 samples/sec 是否稳定？

### 做科研第一要务
要明确问题的本质，就是从机器学习上讲，最基础的东西，这到底是个分类问题还是个回归问题，一定要从问题的本质去出发，然后平时要多读论文，这样在抓住问题的本质之后才能想到对应的比较好的解决方式。AI4S的方法论是要慢于AIcore的，所以作为一名AI科学家也一定要多读AI基础理论的文章，这样才能有一个比较好的理解。